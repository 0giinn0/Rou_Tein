{
  "name": "rou-tein",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test -w packages/shared",
    "lint": "npm run lint -w packages/shared -w apps/mobile -w apps/web"
  },
  "dependencies": {
    "framer-motion": "^12.40.0",
    "three": "^0.160.0"
  }
}
